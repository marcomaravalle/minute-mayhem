# Minute Mayhem — v0.17.1 Playtest

Static playtest build ready for Vercel.

## Deploy
Upload this folder to a Git repository and import it into Vercel, or deploy the folder with the Vercel CLI.

No build command is required. `index.html` is the entry point.
